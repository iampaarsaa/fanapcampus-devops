ssl_certificate /etc/certs/{{ root_domain }}/fullchain.pem;
ssl_certificate_key /etc/certs/{{ root_domain }}/privkey.pem;

server {
    listen 80;
    return 301 https://$host$request_uri;
}

server {
    listen 443 default_server;
    server_name {{ root_domain }};
}
